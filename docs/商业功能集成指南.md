# 商业功能集成指南

**文档版本**: v1.0
**更新日期**: 2026-01-18

---

## 🎯 快速集成指南

### 步骤1: 添加文件到Xcode项目

运行脚本：
```bash
cd "/Users/jiaojunze/Library/Mobile Documents/com~apple~apple~CloudDocs/working_MAC/iphoneInfo"
chmod +x add_files_to_xcode.sh
./add_files_to_xcode.sh
```

### 步骤2: 在现有UI中集成订阅入口

#### 在 `BenchmarkView.swift` 中添加订阅按钮

找到 `// Test Info` 或合适位置，添加：

```swift
// 导入订阅管理
import StoreKit

// 添加状态变量
@State private var showingSubscription = false

// 在合适位置添加订阅按钮
Button(action: { showingSubscription = true }) {
    HStack {
        Image(systemName: "crown.circle.fill")
        Text("升级专业版")
    }
    .font(.subheadline)
    .foregroundColor(.white)
    .padding(.horizontal, 16)
    .background(Color.orange)
    .cornerRadius(10)
}
.sheet(isPresented: $showingSubscription) {
    SubscriptionView()
}
```

#### 在 `SettingsView.swift` 中添加管理订阅选项

```swift
// 在设置项列表中添加
List {
    // 现有设置项...

    // 订阅管理
    HStack {
        Image(systemName: "crown")
        Text("订阅管理")
        Spacer()
        Text("当前：免费版")
            .font(.caption)
            .foregroundColor(.secondary)
        Button("管理") {
            SubscriptionManager.shared.manageSubscription()
        }
    }
}
```

---

## 🔧 功能集成示例

### 1. 在测试结果页添加分享按钮

在 `DetailedReportView.swift` 或结果展示区域添加：

```swift
// 在导航栏工具栏添加分享按钮
.toolbar {
    ToolbarItem(placement: .navigationBarTrailing) {
        Button(action: { shareResult() }) {
            Image(systemName: "square.and.arrow.up")
        }
    }
}

private func shareResult() {
    let result = /* 传递测试结果 */
    ShareSheet(activityItems: [
        // 可选：添加分享文本
        shareText
    ])
}
```

### 2. 在历史记录页添加导出按钮

在 `CompareView.swift` 的历史记录部分添加：

```swift
// 在历史记录列表头部添加导出按钮
HStack {
    Text("历史记录")
        .font(.headline)
    Spacer()

    Button(action: { showingExport = true }) {
        HStack {
            Image(systemName: "square.and.arrow.up")
            Text("导出")
        }
    }
}
.sheet(isPresented: $showingExport) {
    DataExportView()
}
```

### 3. 在监控页面添加排行榜入口

在 `MonitorView.swift` 添加排行榜标签：

```swift
// 添加到监控标签页中
enum MonitorTab: String, CaseIterable {
    case all = "全部"
    case cpu = "CPU"
    case memory = "内存"
    case gpu = "GPU"
    case battery = "电池"
    case leaderboard = "排行榜"  // 新增
}
```

在 `tabPicker` 的 caseIterable 中添加排行榜。

---

## 🧪 测试清单

### 订阅系统测试

- [ ] 能否看到订阅按钮并打开订阅页面？
- [ ] 产品价格是否正确显示？
- [ ] 能否看到7天试用说明？
- ] 点击订阅能否打开系统订阅管理界面？
- ] 购买后功能是否正确解锁？
- ] 订阅状态是否正确更新？

### 功能门控测试

- [ ] 未订阅时是否正确显示锁图标？
- [ ] 锁定的功能是否无法使用？
- ] Pro用户是否无限制？
- ] 激活订阅后功能是否立即解锁？

### 云端排行榜测试

- [ ] 排行榜页面能否正常加载？
- ] 不同榜单类型切换是否正常？
- ] 设备筛选是否正常工作？
- ] 用户排名是否正确显示？
- ] 分页加载是否正常？

### 数据导出测试

- [ ] CSV导出是否包含正确格式？
- [ ] JSON导出是否包含完整数据？
- ] PDF导出是否生成正确布局？
- ] 导出文件能否在系统分享菜单中打开？

### 分享功能测试

- [ ] 卡片生成是否成功？
- ] 不同风格的卡片是否正常渲染？
- ] 文本复制是否正常？
- ] 原生分享是否正常？

---

## 📱 App Store 配置

### StoreKit 2 产品配置

在 App Store Connect 中创建以下产品：

| 产品ID | 类型 | 价格 | 描述 |
|-------|------|------|------|
| `com.iphoneinfo.pro.monthly` | 订阅 | ¥15/月 | 月付订阅 |
| `com.iphoneinfo.pro.quarterly` | 订阅 | ¥38/季 | 季付订阅 |
| `com.iphoneinfo.pro.yearly` | 订阅 | ¥98/年 | 年付订阅 |

**注意**:
- 确保产品ID与代码中的定义完全一致
- 配置7天免费试用期
- 设置订阅组和产品分组
- 上传订阅截图和描述
- 提交审核时选择"自动续订订阅"

### 订阅组配置

**订阅组**: iPhoneInfo Pro

**群组ID**: (自动生成或在App Store Connect手动创建)

**订阅**: 选择所有3个产品

**订阅组信息**:
- 名称: iPhoneInfo Pro
- 描述: 解锁所有专业功能
- 支持的语言: 中文

---

## 🚀 常见问题解决

### 编译错误修复

**错误**: `Cannot find 'SubscriptionManager' in scope`

**解决**:
1. 确保文件已添加到 Xcode 项目
2. clean build (Product > Clean Build Folder)
3. 重启 Xcode

**错误**: `Cannot find 'ProFeatureGate' in scope`

**解决**:
1. 导入正确的文件: `import ProFeatureGate`
2. 确保文件在项目中

**错误**: `Use of unresolved identifier 'SubscriptionManager'`

**解决**:
1. 确保 SubscriptionManager.swift 在项目中
2. 在文件顶部添加: `import SubscriptionManager`

---

## 📞 技术支持

如有问题，请检查：
1. 文件是否已添加到Xcode项目
2. StoreKit 2 是否在Signing & Capabilities中启用
3. 网络连接是否正常
4. App Store Connect配置是否正确

---

**下一步**: 广告SDK集成 (已延后)

